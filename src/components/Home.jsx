import { Button } from "@mui/material";

const Home = () => {
  return (
    <div>
        {console.log('fdjlsk')}
      <Button variant="contained">Test</Button>
    </div>
  );
};

export default Home;
